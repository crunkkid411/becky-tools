# WHORETANA / becky secrets. Copy this file to ".env" (same folder) and fill in.
# The real .env is git-ignored - never committed. Whoretana loads it on startup.

# For the Gemini 2.5 Flash realtime brain (Settings -> Brain -> Gemini).
# Get a key at https://aistudio.google.com/apikey
GEMINI_API_KEY=
