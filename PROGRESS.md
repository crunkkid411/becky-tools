# becky-toolset — Build Progress

Loop tracker for the Ralph build session. Read this first each iteration.

- Started: 2026-06-06
- Spec source: `X:\AI-2\hermes-agent-workspace\09-becky-go-v3-spec.md` (v3, authoritative)
- Task files: `X:\AI-2\hermes-agent-workspace\to-do\` (00-16)
- Build root: `X:\AI-2\becky-tools\becky-go\` (Go 1.26 module `becky-go`)

## Environment (verified 2026-06-06)

| Dependency | Location | Status |
|---|---|---|
| Go | `C:\Program Files\Go` (1.26.1) | OK |
| ffmpeg/ffprobe | `C:\ProgramData\anaconda3\Library\bin` | OK |
| auto-editor | `C:\Users\only1\bin\auto-editor.exe` | OK |
| Python (sherpa_onnx 1.13.2) | `X:\AI-2\kevs-obsidian-ingestion-engine\.venv\Scripts\python.exe` | OK |
| Parakeet-TDT-0.6B-v3 (int8) | `...kevs...\models\asr\sherpa-onnx-nemo-parakeet-tdt-0.6b-v3-int8` | DOWNLOADED + WORKING |
| silero_vad.onnx | `X:\AI-2\becky-tools\models\silero_vad.onnx` | COPIED + WORKING |
| pyannote-segmentation-3.0 | `...kevs...\models\diarization\sherpa-onnx-pyannote-segmentation-3-0` | DOWNLOADED (untested here) |
| GPU | RTX 3070 Laptop (nvidia-smi present) | OK; torch in kevs venv is CPU-only |

Note: the kevs venv's torch is CPU-only and lacks torchaudio, so the legacy
`vad_analyzer.py` does NOT run there. becky uses **Silero VAD via sherpa-onnx**
instead (the v3-spec path) — no torch, no internet, self-contained.

## Tools

| # | Tool | Status | Notes |
|---|------|--------|-------|
| 01 | becky-transcribe | DONE — built + tested on test.mp4 | 45.2s clip in 27s CPU; word ts + segments; json/srt/txt/vtt |
| 03 | becky-cut | DONE — built + tested on test.mp4 | auto-editor + sherpa Silero VAD post-pass + h264_nvenc; dry-run + render verified (45.2s -> 22.79s, removed 2 non-speech segs) |
| 02 | becky-diarize | DONE — built + tested on test.mp4 | sherpa-onnx OfflineSpeakerDiarization (pyannote-seg-3.0 + CAM++); auto speaker count (num_clusters=-1 + threshold); test.mp4 -> 5 speakers/12 segs @ default thr 0.5 (2 @ thr 0.8); --min/--max-speakers pin count; json/srt/txt; valid JSON, exit 0 |
| 04 | becky-vad | DONE — built + tested on test.mp4 | wraps `pyhelpers/vad_silero.py` (new `--full-segments` mode); emits whole-file speech/non-speech timeline w/ per-seg speech_pct + keep; 45s clip -> 25 segs (12 speech, 46.8%), valid JSON, exit 0 |
| 05 | becky-identify | DONE — built + tested on test.mp4 | Voice (CAM++ 192-dim via new `pyhelpers/voice_embed.py`, cosine in Go) + location (aHash via shared internal/osintexport) identification against an enrolled KB; NAMED output ("Defendant" not "Speaker 1") + unidentified[]. Reuses `diarize_sherpa.py` (accepts `--diarized` or runs diarization internally) and `osintexport` AHashFromImage/AHashFromGray64/HammingDistance. Built throwaway KB from test.mp4 (SPEAKER_00 -> Defendant): voice match cosine 0.7573 (>=0.5); SPEAKER_01/02 -> unidentified[]; discrimination proven at `--voice-threshold 0.95` (0 ids, all 5 unidentified, no false match). Location frame enrolled -> "Defendant's home" hamming=6 conf 0.9063; precomputed-hash sidecar path also verified. Face NOW FUNCTIONAL (2026-06-07): InsightFace buffalo_l (SCRFD + w600k_r50 ArcFace, 512-d) via new `pyhelpers/face_embed.py` + `cmd/identify/face.go` (samples frames 3s/cap24, cosine-match, shared vec.go); kb loads `face-prints/<name>/`; config FacePython/FacePyLib/FaceModelRoot/FaceModelName; PYTHONPATH=X:\PythonUserBase\Lib\site-packages. test.mp4 → face "Defendant" 0.8712 cosine (enrolled t=7.5s). Degrades gracefully if models/deps absent. 12 Go unit tests pass; valid JSON, stderr 0 bytes w/o --verbose, exit 0; errors -> exit 1 |
| 06 | becky-events | DONE — built + tested on test.mp4; multi_face WIRED 2026-06-07 | 20% nuance detection: dominant-speaker rule over real diarized.json (SPEAKER_00 dominant -> 7 phone_call events, all non-dominant turns <=20s); location_change via 1fps 8x8-gray aHash + Hamming (0 changes on static clip; threshold-0 forced run wrote 38 full-res .jpg + provenance .json to osint dir, sidecar sha256 == source sha256, frames 720x1280 full-res); shared logic factored into internal/osintexport for becky-osint reuse; valid JSON, stderr quiet w/o --verbose, exit 0. **multi_face NOW FUNCTIONAL (#2b, 2026-06-07):** reuses the SHARED new `internal/faceembed` package (one face-embed runner shared by becky-identify + becky-events — `cmd/identify/face.go` refactored to call `faceembed.Embed`, its duplicated runner/parser/env helpers deleted). becky-events samples frames coarsely (every 3s, cap 24 — NOT the 1fps location frames) via shared osintexport.ExtractFrame, runs faceembed.Embed, and emits a `multi_face` event {type,start,end,timestamp,frame,face_count,confidence,description} for each frame with NFaces>=2 (helper gates det_score>=0.5). New `--no-multi-face` flag; reuses cfg.Device/--device. Default-on but GRACEFUL: missing models/deps OR no video → `notes.multi_face = "skipped: <reason>"`, speaker+location events still ship, exit 0 (verified: bogus face_python → skipped note exit 0; audio-only → "skipped: input has no video stream"). loadDiarized now tolerates empty speakers[] (silent footage). REAL tests: test.mp4 (1 person) → **0 multi_face** (no spurious fire), 0 location, valid JSON exit 0; crafted 2-face hstack mp4 (silent, h264_nvenc) → **1 multi_face event face_count=2 det_score=0.768** exit 0; faceembed unit test on 2-face composite asserts NFaces>=2 (PASS, NFaces=2). identify regression intact: face "Defendant" cosine **0.8712** unchanged after refactor. stderr 0 bytes w/o --verbose; go build/vet/test + gofmt clean; build-all-tools.bat green |
| 07 | becky-osint | DONE — built + tested on test.mp4 | frame export + provenance sidecars; reads becky-events JSON, extracts full-res frame per event (timestamp field if present else start), aHash via shared internal/osintexport (ExtractFrame/SHA256File/AHashFromImage/HashHex/WriteProvenance/Sidecar — NOT reimplemented), writes .jpg/.png + provenance .json sidecar; stdout manifest of {event_type,timestamp,frame_index,frame_path,sidecar_path,perceptual_hash,sha256}. Verified: events.json -> 7 phone_call frames (prefix "speaker"), events_loc.json (--location-threshold 0) -> 45 location_change frames (prefix "location"), 0 skipped both; frames 720x1280 full-res (jpg+png via ffprobe), sidecar source_sha256 == independent SHA-256 of test.mp4, source unchanged; --include-audio writes 16k mono pcm_s16le wav (best-effort), --format png/--quality work; valid JSON both runs, stderr 0 bytes w/o --verbose, exit 0 |
| 08 | becky-embed | DONE — built + tested on real transcript→DB; **4B RESIDENT SERVER added 2026-06-07** | **NEW DEFAULT `--model qwen3-4b`: Qwen3-Embedding-4B (Q5_K_M GGUF, native 2560) served by a resident llama.cpp `llama-server` (`--embedding --pooling last -ngl 99 -c 8192 -ub 8192 :8088`, launched by new `X:\AI-2\becky-tools\start-embed-server.bat`). `embed_text.py` gained an HTTP backend (`--server-url`): POSTs to `/v1/embeddings` (OpenAI) w/ `/embedding` fallback, embeds DOCUMENTS as raw text (`--mode document`), MRL-truncates to the first 1024 dims + L2-normalizes client-side (`--truncate-dim 1024`; llama-server does NOT normalize this path) → keeps the existing `float[1024]` schema unchanged. No per-call model reload (server is resident). New config: EmbedServerURL (`http://127.0.0.1:8088`), EmbedServerModel (GGUF path), EmbedModel default (additive+merge). **Vector-space guard:** new `embed_meta(key,value)` table stores `embed_model=qwen3-4b`; embed REFUSES to add a different model into an already-tagged DB; if the server is unreachable embed FAILS with "start start-embed-server.bat" (exit 1, NO silent 0.6B fallback — different space, would corrupt the index). REAL test (test.mp4 → fresh transcript → `%TEMP%\q4.db`, server up): dim=1024, 9 seg/9 vec/9 fts, sqlite3 vec_length=1024, embed_meta=qwen3-4b, KNN self-match sim **1.0**, used the SERVER (verbose log, no ST load). VRAM observed: model 2749 MiB + KV 1152 + compute 1269 → **~5.4 GB used / 2.4 GB free** on the 8 GB RTX 3070. Guard demos: server-down embed → clear error exit 1, NO db written. Go tests (cmd/embed incl. usesServer + internal/beckydb embed_meta round-trip, real CLI) pass `-race`; vet+gofmt clean. The 0.6B in-process path remains via `--model qwen3-0.6b`. (Original 0.6B path:) Qwen3-Embedding-0.6B (1024-dim, normalized) via embedded `pyhelpers/embed_text.py` (sentence-transformers, offline, cache_folder=cfg.EmbedModelCache). Established the SHARED `internal/beckydb` layer (schema.sql + schema.go + README.md) that becky-search & becky-consolidate import: driver-free SQLite via sqlite3.exe CLI + sqlite-vec `vec0.dll` (no cgo, no go.mod deps), vectors passed as JSON-array text. Schema: `segments`(segment_id PK, source_file, source_sha256, start/end_time, text, speaker_id/name/confidence, needs_review, verified_by, created_at) + `segments_vec` vec0 `embedding float[1024] distance_metric=cosine` + optional `media`. similarity = 1 - cosine_distance. New config fields: Sqlite3, SqliteVecExt, EmbedModelCache (additive + merge). becky-embed reads transcribe JSON, embeds non-empty segments, deterministic segment_id=sha12(source)+":"+idx, INSERT OR REPLACE segments + DELETE/INSERT vectors (vec0 has no upsert). Real test: test.mp4 transcript → 9 segments/9 vectors, stored vec_length=1024, KNN self-match dist 0.0/sim 1.0; idempotent (2nd run still 9/9); valid stdout JSON, stderr 0 bytes w/o --verbose, exit 0; errors→exit 1. Go tests: cmd/embed + internal/beckydb (real CLI+vec0 round-trip) pass; vet clean. **2026-06-07: also populates the new `segments_fts` FTS5 keyword index (gated on `FTS5Available`, delete-then-insert idempotent) for becky-search hybrid retrieval — summary now carries `fts_in_db`+`fts5_available`; test.mp4 → fts_in_db=9 (==segments), re-embed stays 9. Degrades to vector-only if FTS5 absent.** |
| 09 | becky-search | DONE — built + tested on real transcript→embed DB; HYBRID retrieval added 2026-06-07; **4B RESIDENT SERVER + query-prefix + vector-space guard added 2026-06-07** | **NEW DEFAULT `--model qwen3-4b`: query embedded through the SAME resident `llama-server` becky-embed indexed with (`--server-url`, MRL→1024 + L2-norm). QUERIES use the Qwen3 Instruct/Query prefix (`--mode query` → "Instruct: {task}\nQuery: {q}") while DOCUMENTS stay raw — the asymmetry that lifts retrieval (confirmed at runtime: identical text → document vs query vectors cosine 0.749, well below 1.0). **Vector-space guard:** reads `embed_meta.embed_model`; if the query model != the DB's indexed model it FAILS with a clear message (never compares two different embedding spaces); server-unreachable → "start start-embed-server.bat" exit 1 (NO silent 0.6B fallback). REAL test (test.mp4 → 9-seg `q4.db` indexed with 4B, server up): exact phrase "eat a pound of sand" → **rank #1 under --mode hybrid** (matched=hybrid, sim 0.7974, fused 0.0328); paraphrase "how often should I post on social media" → #1 "Can you post ten times a day?" / #2 "posting like four times a day…" (sensible); "is it worth doing something just because you can" → #1 "but should you". Guard demos: query a 4B DB with `--model qwen3-0.6b` → clear mismatch error exit 1; server-down `--model qwen3-4b` → clear "server unavailable" error exit 1. Go tests pass `-race`; vet+gofmt clean. 0.6B path available via `--model qwen3-0.6b`. (Original:) Natural-language search over the SHARED `internal/beckydb` layer (REUSES `Open`/`EnsureSchema`/`KNN`; NO duplicated SQL/schema/KNN). **HYBRID retrieval (keyword BM25 + vector KNN, RRF-fused) added 2026-06-07** — the investigation-review §2.2a/#2 recommendation. Embeds the query with the SAME embedded `pyhelpers/embed_text.py` + Qwen3-Embedding-0.6B (1024-dim, normalized, batch=1) becky-embed indexed with. Maps neighbors → spec schema: rank (1-based, fused/similarity desc), source_file, source_sha256, timestamp(=start), duration(=end-start), speaker (speaker_name or "unidentified speaker" when empty — NOT "Speaker 1"), speaker_confidence, text, similarity(=1-cosine_dist), **matched(hybrid\|vector\|keyword)**, **fused_score(RRF)**, needs_review(bool), verified_by(null when empty) + stats{total_results, named_speakers, unidentified, avg_confidence=mean similarity}. Flags: --db --limit(k) --min-confidence(minSim, filters vector side) **--mode hybrid\|vector\|keyword (default hybrid)** **--rrf-k(60)** --format json|txt --expand --verbose. **FTS5: built into sqlite3 3.51.1 (verified available); new `segments_fts` FTS5 table (porter unicode61) in schema_fts.sql, populated by becky-embed (fts_in_db==seg count), queried via new `beckydb.KeywordSearch` (BM25, sanitized MATCH so arbitrary text incl. `a-b OR c`/unbalanced-quote never errors). RRF fuse in new `cmd/search/fusion.go`: score=Σ 1/(rrf_k+rank). DEGRADES to vector-only + note if FTS5 absent.** Real test (test.mp4 → 9-seg hybrid.db): exact phrase "eat a pound of sand" → **rank #1 under --mode keyword AND --mode hybrid** (hybrid: matched=hybrid, sim 0.760, fused 0.0328); "trial reels" → #1 both modes; paraphrase "how often should I post…" --mode vector unchanged (top sim 0.707 — NO regression). keyword mode 252ms w/ NO model load. Edge cases: empty/new DB → valid JSON 0 results exit 0; special-char/no-hit/punct queries → valid JSON no error; invalid --mode/--rrf-k → exit 1 stderr. idempotent re-embed (seg/vec/fts all stay 9). Valid stdout JSON, stderr 0 bytes w/o --verbose, exit 0; errors→exit 1. Go tests: cmd/search (mapping/stats/format/parse + new fuseRRF/matchedLabel/tagAll) + internal/beckydb (KeywordSearch/InsertFTS/CountFTS/FTS5Available/sanitizeFTSQuery/KNN-rank + no-FTS5 degrade, real CLI) pass `-race`; vet + gofmt clean. Did NOT touch embedding model / face / resident-server code |
| 10 | becky-review | DONE — built + tested on real transcript+events pipeline | The ONE LLM tool (per-media context review). Pluggable `Backend` interface (Name + Review(ctx, in)) with 3 impls in `cmd/review/backend.go`: **mock** (offline, deterministic, the guaranteed test path — emits a notable_moment per real becky-events event w/ its real timestamps + a reference_resolution per transcript segment with a word-boundary pronoun/relationship cue; never matches "he" inside "the"), **claude-code** (shells `claude -p --output-format json --append-system-prompt <sys>`; prompt passed via STDIN — NOT as a -p arg — because the Windows claude.cmd shim mangles large multi-line args through cmd.exe; runs from os.TempDir() so the CLI doesn't auto-discover the project CLAUDE.md and go conversational; parses the CLI envelope `{type,subtype,is_error,result}` then defensively extracts the model's JSON array from result via parse.go [strips ```fences, tolerates prose, {"annotations":[...]} wrapper]; sensitive data stays local), **openrouter** (POSTs OpenAI-compatible chat/completions when OPENROUTER_API_KEY set, else clean skip note). ONE LLM call per media file (never batches — preserves nuance); context.WithTimeout per file (--timeout, default 180s); --concurrency(3) reserved for batch callers. Every annotation carries type/segment_start/segment_end/text/resolution/rationale/confidence/significance/reviewed — normalize() guarantees rationale+significance present & confidence clamped to [0,1] & reviewed=false. `--vision` extracts one full-res key frame per event timestamp via shared internal/osintexport.ExtractFrame (best-effort, never blocks). Case context: --case-context file or built-in defaultCaseContext; example at `config/context-review.prompt.md`. ALL backends DEGRADE GRACEFULLY (CLI missing/timeout/non-zero/unparseable → valid JSON + empty annotations + note, exit 0; never crashes, never half-JSON). REAL tests on test.mp4: transcribe→events→review **mock** → 7 notable_moment annotations w/ real ts (6.038-8.03s etc.), valid JSON, exit 0; **claude-code** → 4 real annotations parsed live (content_summary/no_investigative_content/event_false_positive/no_entity_resolution — model correctly flagged the phone_call events as false positives on a single-speaker monologue & found no case-entity matches), exit 0 (also proven to degrade cleanly to []+note when output unparseable); **openrouter** no key → []+"skipped: OPENROUTER_API_KEY not set", exit 0; `--vision` → 7 full-res frames extracted; error paths (missing --transcript / unknown backend) → exit 1 + stderr; stdout clean JSON, stderr 0 bytes w/o --verbose. 12 Go unit tests (parse/normalize/word-boundary cue/mock determinism/prompt) + `go test -race` pass; vet + gofmt clean; build-all-tools.bat green |
| 11 | becky-export | DONE — built + tested on a REAL becky-cut timeline → REAL MP4 | Renders the becky-cut v1 timeline JSON (`{version,source,chunks:[[start,end,speed]]}`) two ways. **mp4** (default): `auto-editor <timeline.json> -o out.mp4 --progress none -c:v <codec> -b:v <bitrate>` (matches becky-cut's render path exactly); codec defaults to cfg.Codec (h264_nvenc), `--codec hevc_nvenc` alt, libx264 hard-rejected (exit 1). **kdenlive**: `auto-editor <source> --edit audio --margin 0.04s,0.25s --export kdenlive -o out.kdenlive` (Jordan's locked margin) → validated as parseable MLT XML; noted limitation: chunk spans are auto-editor's own audio detection, not the timeline JSON cuts. Flags: --output (default `<source-stem>_edited.mp4`/`.kdenlive`), --format, --codec, --bitrate(12M), --fps(→--frame-rate), --hwaccel(true), --include-subs/--subs (best-effort ffmpeg `subtitles=` burn-in, re-encode with same nvenc codec, drive-colon-escaped path; graceful skip+note when SRT missing), --verbose. JSON summary: {input_timeline, source, output, format, codec, bitrate, hwaccel, chunks, rendered, output_duration, output_resolution, output_mb}. REAL test: becky-cut --keep-temp --no-vad → `%TEMP%\becky_cut_timeline_*.json` → export → `export-test.mp4` 36.6MB, ffprobe codec_name=h264, 720x1280 (vertical), duration 23.791s>0 (libx264 NOT used); stdout valid JSON (python json.load); kdenlive → root tag `mlt`, valid_xml true; real SRT burn-in → subs_burned true, still h264; edge cases (missing/garbage/no-source/empty-chunks/no-arg/unknown-format/libx264) → clean stderr + exit 1, 0 bytes stdout (no half-JSON); stderr 0 bytes w/o --verbose. 6 Go unit tests (`go test -race`) + vet + gofmt clean; build-all-tools.bat green |
| 12 | becky-consolidate | DONE — built + tested on real identify→DB pipeline | Name propagation + per-entity coverage report over the SHARED `internal/beckydb` layer (REUSES Open/EnsureSchema; NO parallel DB). Additively EXTENDED beckydb with the `identifications` table (id PK deterministic sha12(source)+":"+modality+":"+key, source_file/sha256, entity_name, modality voice\|face\|location, confidence, speaker_id, verified_by NULL=unconfirmed, created_at) + indexes + helpers (UpsertIdentification, ListIdentifications, SetIdentificationVerified [the propagation write], DistinctSourceFiles [coverage denominator = media ∪ identifications], CountIdentifications); `CREATE TABLE IF NOT EXISTS` so embed/search are UNAFFECTED (verified: transcribe→embed→search still 9 seg/9 vec/top-sim 0.639; all 4 tables coexist in one DB). Two modes: (1) `--ingest <identified.json> --source <video> [--verified-by <name>]` loads a real becky-identify result (each identifications[] entry → a row; --verified-by marks confirmed, omitted = unconfirmed; idempotent via deterministic id) — the identify→DB bridge; (2) default report: per-entity coverage (overall + per-modality distinct-video counts + %), gaps (<50% recognition → deterministic templated suggestions, NO LLM), and propagation (each CONFIRMED entity's name → that entity's UNCONFIRMED rows with conf ≥ --threshold; counts propagated vs skipped; never invents a name where no confirmation exists). Flags: --db --threshold(0.8) --dry-run --format json\|txt --verbose. REAL test: becky-diarize → becky-identify (KB: SPEAKER_00→Defendant) → identified.json (3 voice ids @ 0.7574/0.7172/0.5838 + 2 unidentified) → ingest → report shows Defendant voice 1/1 (100%); 2nd source ingested --verified-by analyst → dry-run @ thr 0.7 lists 2 propagated/1 skipped WITHOUT writing (sqlite3: video1 still NULL), real run @ thr 0.7 sets verified_by=becky-consolidate on SPEAKER_00/03, leaves SPEAKER_04 (0.5838<0.7) NULL; unconfirmed-only "The Wife" (conf 0.99) NEVER propagated (stays NULL). Empty DB → valid JSON/clean txt, exit 0, no crash; bad --ingest/--threshold/--format → exit 1 (stderr). Valid stdout JSON (python json.load), stderr 0 bytes w/o --verbose. 12 Go unit tests (cmd/consolidate) + identifications round-trip (internal/beckydb, real CLI) pass; vet + fmt clean; build-all-tools.bat green |
| 19 | becky-web2md | DONE — built + tested on real live URLs | Web→clean-markdown+YAML-frontmatter. Go CLI + embedded `pyhelpers/web2md.py` (trafilatura + markdownify + beautifulsoup4 + pyyaml + lxml; pip-installed into anaconda python; new `cfg.Web2mdPython` auto-detect). Flags: --output/--vault/--extract article\|full\|links\|tables\|code/--images/--images-dir/--metadata/--tags/--batch/--retry/--fallback/--timeout/--css/--debug. Preserves fenced code blocks (lang recovered from <pre>/<code>/highlight-* wrappers, re-attached by content-signature), GFM tables, headings, links; local image download + path rewrite; auto-name from title slug into --vault. Tested live: Wikipedia/Markdown (trafilatura conf 0.99, 2305 words, valid frontmatter), GitHub README bubbletea (8 ```go + 1 ```shell fences), docs.python.org/json (9 ```python + 2 ```bash); --images downloaded 3 real PNGs w/ rewrite; --batch 2 URLs → 2 files; bad/404/malformed URL → {skipped,reason} valid JSON exit 2 (no crash). Playwright/site-specific `--fallback` DEFERRED (fast-path sufficient on tests; flag accepted, best-effort). Edited shared `pyhelpers.go` (+Web2md embed) & `config.go` (+Web2mdPython). Valid stdout JSON, stderr quiet w/o --verbose |
| 20 | becky-deslop | DONE — built + tested on real text | Deterministic AI-writing-tell remover (NO LLM): 179 embedded regex rules across ALL 17 categories (ported from the `anti-ai-editor` skill's cliche-replacements + detection-patterns). stdin/files → cleaned text, or `--json` findings {category,line,col,match,suggestion}. Flags: --stdin/--output/--format minimal\|full\|aggressive/--check(CI: exit 1 if tells)/--json/--rules FILE(override)/--no-color/--verbose. Markdown-aware: NEVER edits fenced/inline code or YAML frontmatter (proven). Real test: slop paragraph → 27 findings/10 categories, cliches replaced (e.g. "delve into"→examine, "serves as a"→"is a", curly quotes→straight); --check exit 1 on slop / 0 on clean; code-fence content untouched; stdin pipe + --rules override work. Pure Go stdlib, NO shared-file edits. `go test -race -cover` pass, 75.6% coverage |
| 21 | becky-debt-scan | DONE — dogfooded on becky-go | Code technical-debt scanner (9 categories, NO LLM): pure-Go core (Go via `go/ast` exact; Python/Rust/TS/JS via comment-masking lexer heuristics) covering stale TODOs (age via `git blame --porcelain`), unused imports, dead code, missing type hints, deprecated funcs, naming inconsistencies, cyclomatic complexity, duplicate blocks (normalized N-line SHA1), missing docstrings. External linters best-effort (probed; `go vet` present + auto-run; staticcheck/vulture/radon/eslint absent → clean "skipped: not installed" note, scan still succeeds). Flags: --output text\|json\|markdown/--languages/--categories/--min-age/--max-complexity/--since GIT_REF/--fix(gofmt -w only, conservative)/--fix-dry-run/--config .becky-debt.yaml/--ci. Real test: scanned becky-go → 75 files, ~648 findings, valid JSON, file:line VERIFIED real (cmd/consolidate/consolidate_test.go:44 = the named func); --since narrows to git-changed files (degrades cleanly on non-git); --ci severity gating works; --fix-dry-run writes nothing (hash unchanged). Pure Go stdlib, NO shared-file edits; `go test -race` pass |
| 22 | becky-validate | DONE — gemma4-local WORKING + prompts now PLAIN-LANGUAGE per FORENSIC-OUTPUT-PHILOSOPHY.md | Specialty OFFLINE audio-visual "validate flagged moments" LLM-tool (2nd sanctioned LLM exception after becky-review). `cmd/validate` + shared `internal/avlm` (reusable Gemma-4 runner so a future video-editing tool reuses the same model). Backends: **gemma4-local** (default — Gemma-4 E4B-it Q4_K_M GGUF + BF16 mmproj via llama.cpp `llama-mtmd-cli`, GPU `-ngl 99`, ≤30s audio/≤60s video, `[t.s]` timestamps baked into captions), **fusion** (degrade), **mock** (deterministic CI). Offline-only (no remote backend), candidate-not-conclusion disclaimer, `tone_vs_content_flag`. config: GemmaModel/GemmaMMProj/LlamaMtmdCli/LlamaServer. llama.cpp updated b8468→**b9551** (mtmd present; old at `bin.bak-3306dba`). **gemma4-local WORKING (2026-06-07): real cross-modal answers on test.mp4 (30 frames + 30s audio, ~18s GPU inference, VRAM ~5.2GB). Fixes: llama-server transport (llama-mtmd-cli crashes on Gemma-4), `enable_thinking=false` (the answer was in Gemma-4's hidden thinking channel), and server context 8192→16384 + MaxFrames cap 40 (the default 30-frame prompt overflowed 8192 → HTTP 400 empty). NOT the #24251 NaN bug (that's Blackwell/compute-12.0 only; the RTX 3070 is fine on full GPU offload). Runs on the prebuilt b9551 (no binary swap needed).** **PROMPT RE-TUNE (2026-06-07) — output is now PLAIN LANGUAGE per `FORENSIC-OUTPUT-PHILOSOPHY.md`:** removed all anatomical/Latin jargon (no more "iliac crest / gluteal / lateral / pelvic") from both stages of `cmd/validate/prompt.go`; the model now uses plain words (butt/hip/waist/thigh/chest/back), names people plainly ("the man / the woman", never codes), narrates the ACT and its dynamics across time + the receiving person's RESPONSE, and a downward-gaze / pull-away OVERRIDES "neutral" (was being flattened). Output merged to a 4-8-obs phase timeline that reads as a story. Verified on `E:\TakingBack2007\test\test.mp4` (warm `--server-url`): contact arc back→shoulder→hip→waist→lower-back + the **0:20-0:24 downward-gaze distress cue captured** (old jargon output missed it). Honest ceiling (verified by frame probes): Gemma-4-E4B canNOT resolve the 0:13 butt-tap (motion-blurred low hand blended into tattoos/dark clothing), the forced pull-in (force invisible in 1-fps stills), or the disguised hand-removal as resistance (reads as mutual contact); these are below the model's perception ceiling and reported as such, not faked. Refreshed sample at `becky-go/eval/example-validate-test.mp4-output.json`. mock verified (valid JSON + disclaimer + tone flag); `go vet ./...` + `go test ./...` clean; `build-all-tools.bat` green. |
| 23 | becky-pipeline | DONE — verified on test.mp4; **SIDECAR INGESTION added 2026-06-07** | One-command resumable forensic-chain orchestrator. `cmd/pipeline` (thin; shells the becky-*.exe; imports beckyio/config/beckydb/mediainfo + new `internal/sidecar`; bin via os.Executable + `--bin`). Flags: `<video\|folder> --out --kb --bin --steps --db --resume --force --force-transcribe --lang --verbose`; default steps **transcribe,metadata,diarize,events,osint** (embed/identify opt-in). **NEW — reuse what yt-dlp already downloaded instead of re-transcribing 500 GB:** the transcribe step first looks for a subtitle sidecar next to the video (`<stem>.en.srt` > `.srt` > `.en.vtt` > `.vtt` > `.json3`, case-insensitive, exact-stem priority then `<stem>.<lang>.*` fallback) via new shared `internal/sidecar` (SRT/VTT/json3 parser; **collapses YouTube rolling-caption overlap** into clean monotonic segments by dropping each cue's shared word-prefix); if found (and not `--force-transcribe`) it writes `transcript.json` in the EXACT becky-transcribe shape (file/duration/model/lang/text/words[]/segments[]) stamped `transcript_source:"youtube-srt"` + `sidecar_path` and **SKIPS Parakeet**; no sidecar → becky-transcribe (Parakeet) runs and its transcript.json is stamped `transcript_source:"parakeet"`. New in-process **metadata** step parses `<stem>.info.json` (→ `metadata.json` + DB `media_meta`: upload_iso RFC3339 timeline anchor, uploader/channel+ids, title, description, duration, chapters, webpage_url, video_id) and `<stem>.live_chat.json` (→ DB `live_chat`: author+text+offset_sec, deterministic chat_id) so both are searchable/cross-referenceable; degrades to a graceful skip when no sidecar. `beckydb` additively extended (`media_meta`+`live_chat` tables + UpsertMediaMeta/InsertLiveChat/Count*; CREATE IF NOT EXISTS → embed/search unaffected). Resumable, per-step graceful failure, JSON manifest now carries per-step `transcript_source`+`note`. **REAL verification (small CLANCY video copy + REAL yt-dlp sidecars renamed to its stem):** transcribe → `transcript_source=youtube-srt`, reused `.en.srt` (892 segments, Parakeet skipped), real lines ("Boom." 2.8s / "We're doing the live streams." 9.92s); metadata → `media_meta`(id 4W1vwvpXdt4, Puff the Daddy Dragon, upload_iso 2026-01-06T23:26:50Z, url) + 15 `live_chat` rows; embed 892/892 vec+fts; **`becky find "live streams"` → rank #1 @ 9.92s, "getting DDoSed" → "continuously get DDoSed" @ 53.44s**. No-sidecar fallback (test.mp4) → `transcript_source=parakeet` (9 seg/71 words), metadata gracefully skipped. Also fixed becky-embed: large transcripts (>28 KB b64) now pass texts via a temp file (`embed_text.py --texts-b64-file`) to dodge the Windows 32 KB cmdline limit. 18 unit tests (pipeline ingest + sidecar parsers + beckydb meta round-trip); vet + gofmt clean; build-all green. |
| 24 | becky-eval | DONE — built + real result on 2 example videos | Auto-research / recall-scoring harness (`cmd/eval`). Reads a manifest pairing each video with wiki answer-key FACTS (alias lists + weights + categories) + a config/prompt search space; runs the tool per (case×config), flattens output JSON to a recall haystack, scores **weighted recall** (false positives NOT penalized — forensic recall-first), ranks configs on the train split, re-measures the best on HELD-OUT cases (generalization guard). Offline, deterministic scoring, resumable (per-case disk cache). Real result (`eval/example-report.json`): test.mp4 physical-contact recall 0.667; vid-jan2 transcribe recall 0.727 (holdout) — surfaced a real gap: Parakeet ASR mis-renders "hair jordan". Manifest `becky-go/eval/forensic-manifest.json`. Used to tune becky-validate's two-stage forensic prompt (see row 22). go vet + go test green; build-all-tools.bat (18 tools) green. Caveat: tuned on 2 clips — illustrative, not benchmarks; expand the manifest before trusting rankings. |
| 25 | becky-enroll | DONE — built + verified on the REAL CLANCY wiki (2026-06-07) | **Auto-enrollment: wiki → becky-identify KB, zero human clip-making** (spec Deliverable A). `cmd/enroll` (main/wiki/enroll/runners/kb + tests). Crawls the case wiki `.md` files, detects PERSON entities (`tags:` has `person`, or `entity`+`primary`; H1 = name; `Known Aliases` table + `aliases:` → aliases), and resolves the raw video/image files each references (direct refs + one evidence-file hop via person-slug tags / `Primary speaker` field / body-centered dominance; evidence scoped per-wiki-root so the two CLANCY wikis don't cross-bleed; videos ranked by exclusivity so a person's dedicated solo video beats a shared exhibit). Per person: shells **becky-diarize** (REUSED, not reimplemented), picks THAT person's cluster (single-speaker → direct; `SPEAKER_NN=Name` hint → that cluster; else dominant), ffmpeg-clips a ~15-30s clean single-speaker span → `KB/voice-prints/<Name>/*.wav`; samples frames (osintexport.ExtractFrame) + shared **internal/faceembed** → picks the highest-det_score SINGLE-face frame → `KB/face-prints/<Name>/*.jpg`. **Non-subject guard:** persons tagged attorney/prosecution/officer/judge are SKIPPED by default (their wiki media are case EXHIBITS, not recordings of them) — `--include-non-subjects`/`--only` overrides. Writes the becky-identify KB layout (`entities/<name>.json`), `enrollment-registry.json` (spec shape: generated ISO date, wiki_roots, per-entity enrolled{voice,face}/voice_clip/face_image/skip_reason, summary), and `enrollment-report.txt`. People with no clean sample → SKIP + recorded reason (never fabricated). Flags: `--wiki`(repeatable) `--kb` `--bin` `--only` `--no-face` `--no-voice` `--include-non-subjects` `--device` `--dry-run` `--verbose`. **REAL verification (CLANCY-TRIAL wiki, kb-final):** 9 people detected → 3 real subjects FULLY enrolled from DISTINCT videos (John Anthony Clancy ← ScreenRecording_01-09; Shelby ← VID_20260102; Hair Jordan ← IMG_6832), 2 attorneys skipped (non-subject), 4 skipped (no media on disk). **Self-match (re-ran becky-identify with the new KB):** John voice **0.7595** / face **0.9442**; Shelby voice **0.7668**; Hair Jordan voice **0.5819** — all > 0.5. VIDEOS wiki (test.mp4): John Clancy + Shelby both enrolled, self-match voice **0.9105** / face **0.998**. Known limitation (honest): when 2 people share one video that diarizes to a single speaker (test.mp4; VID_20260102), they collide on voice/face — a human must confirm which print is correct. go build/vet/test (`-race`) green; in build-all-tools.bat. |
| 26 | becky (orchestrator) | DONE — `becky profile` verified end-to-end (2026-06-07) | **The "brain": plain-language ops that CHAIN the existing tools** (spec Deliverable B). `cmd/becky` (main/runner/ops/report + tests); ships as **bin\becky.exe** (no `becky-` prefix — special-cased in build-all-tools.bat). Thin driver: shells the becky-*.exe (resolved next to itself or `--bin`), never reimplements. Ops: `enroll-wiki` → becky-enroll; `index <dir>` → becky-pipeline (**transcribe,metadata,embed** — REUSES yt-dlp `.srt`/`.vtt`/`.json3` subs (skips Parakeet) + ingests `.info.json`/`.live_chat.json` where present, falls back to Parakeet otherwise; `--force-transcribe` forces verbatim Parakeet; needs the embed server, degrades gracefully); `find "<q>"` → becky-search (hybrid); `appearances "<name>"` → becky-identify across the corpus → coverage rollup ("<name> recognized in N/M videos" with the actual clip/frame timestamps); `profile "<name>"` → KB card + appearances; `corroborate "<claim>"` → becky-search → candidate supporting moments for review (candidate-not-conclusion disclaimer; `reviewed:false`). Every op: plain-English headline to stderr + structured JSON to stdout (+ `--json` to suppress the headline). **REAL end-to-end:** `becky profile "John Clancy" --kb kb-videos --corpus test.mp4` → headline "Profile: John Clancy — enrolled=true, recognized in 1/1 video(s)." + JSON card (voice_prints:1, face_prints:1) + appearances (voice cosine 0.9105 @ SPEAKER_00, face cosine 0.998 @ 36s). go build/vet/test (`-race`) green; in build-all-tools.bat. |
| 27 | becky-framematch | DONE — built + verified on REAL CLANCY same-room videos + a synthetic fair test (2026-06-07) | **FRAME-MATCHING visual-comparison EXHIBIT builder** (the tool form of the hand-made `2601-Chatham-Cir-Occupancy-Logs.pdf`; `FORENSIC-OUTPUT-PHILOSOPHY.md` part 2). `cmd/framematch` (main/frames/pairing/enhance/layout/manifest/manifest_io + tests). Given TWO sources (two videos, OR a video + a folder of reference images) it: (1) **samples + perceptual-hashes** frames from both (REUSES `internal/osintexport` ExtractFrame/AHashFromImage/HashHex/SHA256File/WriteProvenance/Sidecar — same provenance format as becky-osint/events; `--interval`/`--fps`); (2) **pairs across sources by LOW Hamming distance** (`--threshold`, greedy 1:1 closest-first so each scene appears once, ranked, `--max-pairs`); (3) **honest enhance (optional, logged)** — brightness/contrast/gamma/saturation via ffmpeg `eq` applied to a COPY (`--enhance --brightness/--contrast/--gamma/--saturation --enhance-side a\|b\|both`); FORBIDDEN-by-construction: no warp/stretch/diffusion/clone flag exists; every edit recorded in the manifest + shown in the exhibit's per-page edit log; (4) **labeled side-by-side exhibit** — per-pair PNG (ffmpeg scale→header-band→hstack→drawtext: source file, timestamp, hamming, "what to look for") AND a self-contained `exhibit.html` (source provenance cards w/ sha256, one clear comparison per pair, relative img paths so the dir is portable); (5) **re-runnable JSON manifest** {source_a/b path+sha256, interval/fps/threshold/max_pairs, enhance_applied, per-pair rank/hamming/similarity/what_to_look_for/frame_a+b timestamps+sidecar paths/comparison_image/enhancements[]} to stdout/`--output` + `<dir>/manifest.json`. Candidate-not-conclusion: NEVER declares "same place". Only READS sources (verified sha unchanged). **REAL test:** `becky-framematch 2026-01-02_YouTube_Livestream_Threat_From_Home.mp4 VID_20260102_200149.mp4 --output-dir framematch-clancy --interval 2 --threshold 14 --max-pairs 6` → 6 candidate pairs (top ham=12 sim=0.81); pair_01.png inspected = the SAME man in the SAME room (same ceiling fan, dark textured wall, decor) from two different cameras same date — the occupancy-log use case, labels+footer readable; exhibit.html valid (6 pair cards, sha256, "what to look for", relative imgs all resolve). **Synthetic FAIR test** (one test.mp4 frame → normal vs mildly under-exposed same-framing copy + 2 real different-content frames): pHash PAIRS the under-exposed scene (ham **3**) while ALL non-matches (ham 15/30/32) are REJECTED at threshold 8 (zero false pairs); `--enhance --enhance-side b` brightened the dark copy → hidden shirt/tattoo/wall detail VISIBLE in the exhibit (before `B_0000.png` near-black; after `B_0000_enhanced.png` recovered), edit logged in manifest, both source images sha256 UNCHANGED. Error path (missing source → exit 1, 0-byte stdout), zero-pairs (→ exit 0, `pairs:[]`, guidance in HTML), stderr 0 bytes w/o --verbose. 11 Go unit tests (pair-ranking closest-first/threshold-reject/greedy-1:1/max-cap, hash+Hamming, enhance active/filter, enhance-logged+source-untouched integration, what-to-look-for, timeLabel) pass `-race`; go vet ./... + gofmt clean; build-all-tools.bat green (21 binaries). **Honest limits:** aHash is COARSE (8x8 luma) — false-pairs similar-but-different rooms; flat/solid frames hash to all-zero (color-blind); heavy gamma-crush or large crop shifts can break a true match (enhance/align first); NO auto-alignment (crop/rotate is manual/deferred — never auto-warp); a human MUST confirm each candidate + corroborate (realtor/listing/witness). |

## How to verify the current build

```bat
cd X:\AI-2\becky-tools\becky-go
build-all-tools.bat
cd X:\AI-2\becky-tools
becky-go\bin\becky-transcribe.exe test.mp4 --verbose
becky-go\bin\becky-cut.exe test.mp4 --dry-run --verbose
```

## Status — 21 TOOLS (2026-06-07)

12 forensic-video tools (01–12) + 3 utility (19 web2md, 20 deslop, 21 debt-scan) +
3 new (22 becky-validate, 23 becky-pipeline, 24 becky-eval) + the orchestration "brain"
(25 becky-enroll = wiki→KB auto-enroll, 26 **becky** = plain-language op runner) +
27 **becky-framematch** (frame-matching visual-comparison exhibit builder).
`build-all-tools.bat` builds all **21** tool `.exe`s (+ becky.exe orchestrator);
`go vet ./...` + `go test ./...` clean.
- **becky name-resolution fixed (orchestrator finalize):** `becky profile "John Clancy"` was
  resolving to "Bettina Burke-Clancy (John Clancy's mother)" (descriptive parenthetical +
  greedy substring) and mis-crediting John's prints. Rewrote `cmd/becky/report.go` matcher to
  strip parentheticals + match whole-word tokens (no raw substring) + rank best match + count
  prints for the resolved entity only. Now "John Clancy"/"John"→John Anthony Clancy (enrolled,
  v1/f1); "Shelby"→Shelby; "Clancy"→ambiguous (3 Clancys; picks one, no fabricated prints —
  surfacing "which Clancy?" is a known limitation). Regression tests added.
- **Calibrated on Jordan's real clips:** identify `--voice-threshold`/`--face-threshold`
  defaults → **0.45** (same-person 0.84 voice / 0.60 face vs different-person 0.03 / 0.32).
- **Embedding upgrade:** Qwen3-Embedding-4B via resident `llama-server` (start-embed-server.bat),
  MRL→1024, hybrid keyword+vector search; llama.cpp updated to b9551 (embedding server
  re-verified). Run the server before embed/search with the default qwen3-4b model.
- **becky-validate** is built + graceful but its Gemma-4 audio-visual backend is BLOCKED by
  upstream llama.cpp #24251 (NaN logits) — it degrades cleanly; a drop-in binary swap will
  light it up once the upstream fix ships (no code change). Only `mock` yields real output today.
- Everything else verified on real inputs (test.mp4 / real clips / live URLs / becky-go repo).

```
transcribe → (vad) → diarize → events → osint
diarize → identify → consolidate
transcribe → embed → search
transcribe + events → review
cut → export
```

Possible follow-ups (NOT required by v3 spec):
- Add the ArcFace face model so becky-identify/events face matching activates
  (currently degrades gracefully — no model present, onnxruntime missing).
- Wire becky-identify to write its results into `identifications` directly
  (today becky-consolidate `--ingest` bridges identify → DB).
- Multi-video corpus runs to exercise becky-consolidate coverage at scale.

## Decisions / deviations (with rationale)

- **VAD engine = sherpa-onnx Silero, not torch `vad_analyzer.py`.** v3 spec says
  "Silero VAD (via sherpa-onnx)"; kevs venv lacks torchaudio; sherpa path is
  self-contained and 10x faster in practice. Legacy `vad_script` config field
  retained as an optional override.
- **Python helpers are embedded** (`internal/pyhelpers/*.py` via `//go:embed`)
  and materialized to `%TEMP%\becky-pyhelpers\` at runtime, so each `.exe` is
  self-contained. Compute stays in sherpa-onnx C++/ONNX; Python is glue only.
- **becky-transcribe default device = cpu.** Guarantees it runs (int8 Parakeet is
  fast on CPU). `--device cuda` available if onnxruntime-gpu is installed.
